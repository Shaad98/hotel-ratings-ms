server:
  port: 8084

spring:
  application:
    name: config-server
  
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Shaad98/hotel-ratings-ms-config.git
          clone-on-start: true
